name: CD

# main 的 CI 成功 → 构建并发布 Docker 镜像到 GHCR（部署见 deploy/README.md）
# 打 v* tag → 额外把前后端构建产物挂到 GitHub Release
on:
  workflow_run:
    workflows: [CI]
    branches: [main]
    types: [completed]
  workflow_dispatch:
  push:
    tags: ['v*']

permissions:
  contents: write
  packages: write

concurrency:
  group: cd-${{ github.event.workflow_run.head_sha || github.ref }}
  cancel-in-progress: false

jobs:
  images:
    name: 构建并发布镜像（GHCR）
    if: github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          ref: ${{ github.event.workflow_run.head_sha || github.sha }}
      - uses: docker/setup-buildx-action@v4.3.0
      - name: 登录 GHCR
        uses: docker/login-action@v4.6.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: 后端镜像
        uses: docker/build-push-action@v7.3.0
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/wemove-course-team/wemove-backend:latest
            ghcr.io/wemove-course-team/wemove-backend:${{ github.event.workflow_run.head_sha || github.sha }}
          cache-from: type=gha,scope=backend
          cache-to: type=gha,scope=backend,mode=max
      - name: 前端镜像
        uses: docker/build-push-action@v7.3.0
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/wemove-course-team/wemove-frontend:latest
            ghcr.io/wemove-course-team/wemove-frontend:${{ github.event.workflow_run.head_sha || github.sha }}
          cache-from: type=gha,scope=frontend
          cache-to: type=gha,scope=frontend,mode=max

  release:
    name: 发布构建产物（v* tag）
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7.0.1
      - uses: actions/setup-node@v7.0.0
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json
      - name: 构建后端产物
        run: npm ci && npm run build
        working-directory: backend
      - name: 构建前端产物
        run: npm ci && npm run build
        working-directory: frontend
      - name: 打包
        run: |
          cd backend && zip -qr ../wemove-backend-dist.zip dist package.json package-lock.json && cd ..
          cd frontend && zip -qr ../wemove-frontend-dist.zip dist && cd ..
      - name: 发布 Release
        uses: softprops/action-gh-release@v3.0.3
        with:
          files: |
            wemove-backend-dist.zip
            wemove-frontend-dist.zip
