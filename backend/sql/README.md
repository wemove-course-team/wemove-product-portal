# 演示SQL使用边界

`init_schema_and_data.sql` 是已有的MySQL演示初始化脚本，包含建库、DROP TABLE、重建表和演示数据。保留用于理解现有数据，禁止直接运行在共享库、生产库或含重要数据的数据库上。

本轮仅规划，没有执行SQL。课程真实后端由B03为新空库建立独立增量迁移与seed；迁移目录在实现时创建，已应用的迁移不得改写。

旧脚本中的价格、公司、用户信息和订单状态属于示例；不作为已成交、已付款或经销商资质证据。参考 [数据库设计](../../docs/planning/database.md)。
