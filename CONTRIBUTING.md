# 团队开发与评审规范

## 开始一个任务

领取有明确验收标准的 Issue；确认前置任务已完成，说明计划修改的领域和公共文件。每人同时进行的实现任务不超过 1 个，另可评审 1 个 PR。阻塞超过半天在 Issue 写明原因和需要的接口。

从最新 main 创建短分支，一个 PR 交付一个可验证结果。默认分支名为 `<领域>-<GitHub账号>-<简述>`；Codex 创建的分支保留 `codex/` 前缀。提交与 PR 标题采用 `<领域>-<GitHub账号>: <简短中文说明>`。现有 `feat/...` 和历史 Conventional Commits 不重写。

```powershell
git status
git switch main
git pull --ff-only origin main
git switch -c product-Snowed-night-catalog-api
# 完成并检查，只暂存本任务文件
git diff --check
git add <本任务文件路径>
git commit -m "product-Snowed-night: 实现产品分页接口"
git push -u origin product-Snowed-night-catalog-api
```

示例中的路径须替换成实际文件。存在未提交改动时先由作者妥善处理，禁止覆盖、批量还原或强推。

## Definition of Ready

- 需求有来源或规划编号，标明课程必需、项目增强或长期能力。
- 单一负责人、估算、前置依赖、涉及文件/接口/表、可复现验收步骤齐全。
- API 与数据库变更有消费者及迁移评审人，敏感字段的访问范围明确。
- 课程实现任务以2–4小时为宜，超过半天拆成更小 PR；远景 Epic 到对应阶段再细化。

## Definition of Done

- 前台、后台与真实 API/持久化形成该任务承诺的闭环；Mock 任务必须显式标记。
- 正常、空结果、失败、越权等相关场景有证据；UI 改动附手机和桌面截图。
- 执行已有的构建及本任务新增测试；PR 填写命令、结果、未执行项。
- 接口、迁移、文档同步；数据库变更说明升级顺序、回滚/向前修复办法。
- 一位非作者、具有写权限的成员审批，讨论解决，已启用 CI 通过后合并。

## 公共变更与评审

共享路由、布局、tokens、HTTP 客户端由 lizhikeer 协调；数据库由 Snowed-night 协调；身份安全由 cy0207kaw 协调；发布由 chenyi-c 协调。这是评审责任，不是禁止协作的文件所有权。

公开 API 字段删除/改名、鉴权规则、数据库破坏性变更、框架替换和部署拓扑变化必须先记录 ADR，至少取得领域负责人和消费方认可，再更新基线版本。新增可选字段优先向后兼容。

`main` 已配置至少一位审批且管理员同样受限。此次不降低保护、不自动合并。CODEOWNERS 用于分派评审；当前仓库尚未强制 code owner 审批及必需 CI。构建在 main 首次成功后，由平台治理 Issue 启用必需检查，避免配置不存在的检查名。

## 与既有 PR 的关系

PR #1《docs(platform): 添加团队协作与版本规范》尚未合并。本文件保留其纵向分工、最小变更、PR 审批、Mock 边界和共同验收原则，并补全实名账号映射、真实后端阶段与长期计划。PR #1 不在本轮关闭或覆盖；M0 治理任务比较两者，把 `CONTRIBUTING.md` 保留为唯一规范入口，旧文件合并时改为指向本入口，避免同时维护矛盾版本。

## 发布与工作量

版本从规划基线 `v0.1` 演进到课程 `v1.0`；文档版本不等于软件 Release。Tag/Release 只指向通过验收的 commit。原有 npm 包版本暂不改。

每个 Issue 记录估算点、实际投入、PR、测试与演示证据。实际工作量占比在答辩前由七人核对形成；计划点数只用于排期，不直接作为成绩证明。
